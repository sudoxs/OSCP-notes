وصل می‌شوی:

ftp 192.168.50.62

name: Anonymous

بعد:

binary  
passive  off (ftp -A 192.168.163.53)
put payload.exe

|دستور|کاربرد|مثال|
|---|---|---|
|`open`|اتصال به یک سرور FTP|`open 192.168.1.10`|
|`user`|وارد کردن نام کاربری و رمز|`user offsec`|
|`ls`|نمایش لیست فایل‌ها و پوشه‌ها|`ls`|
|`dir`|نمایش لیست فایل‌ها (مشابه ls)|`dir`|
|`pwd`|نمایش مسیر فعلی روی سرور|`pwd`|
|`cd`|تغییر پوشه در سرور|`cd uploads`|
|`lcd`|تغییر پوشه در سیستم محلی|`lcd /home/kali/Desktop`|
|`get`|دانلود یک فایل از سرور|`get file.txt`|
|`mget`|دانلود چند فایل|`mget *.txt`|
|`put`|آپلود فایل به سرور|`put shell.exe`|
|`mput`|آپلود چند فایل|`mput *.txt`|
|`delete`|حذف فایل در سرور|`delete test.txt`|
|`mkdir`|ساخت پوشه در سرور|`mkdir newfolder`|
|`rmdir`|حذف پوشه در سرور|`rmdir test`|
|`rename`|تغییر نام فایل|`rename old.txt new.txt`|
|`binary`|تغییر حالت انتقال به باینری|`binary`|
|`ascii`|تغییر حالت انتقال به متنی|`ascii`|
|`passive`|فعال/غیرفعال کردن حالت passive|`passive`|
|`status`|نمایش وضعیت اتصال|`status`|
|`help`|نمایش راهنمای دستورات|`help`|
|`quit`|خروج از FTP|`quit`|
|`bye`|خروج از FTP|`bye`|

| ویژگی                            | **Active Mode (PORT)**                         | **Passive Mode (PASV / EPSV)**     |
| -------------------------------- | ---------------------------------------------- | ---------------------------------- |
| چه کسی اتصال data را شروع می‌کند | **سرور → به کلاینت**                           | **کلاینت → به سرور**               |
| پورت کنترل                       | کلاینت → سرور پورت **21**                      | کلاینت → سرور پورت **21**          |
| پورت انتقال داده                 | سرور از **پورت 20** به کلاینت وصل می‌شود       | سرور یک **پورت تصادفی** باز می‌کند |
| مناسب برای NAT                   | ❌ مشکل دارد                                    | ✅ بهتر کار می‌کند                  |
| مشکل فایروال                     | فایروال کلاینت ممکن است اتصال سرور را بلاک کند | کمتر مشکل دارد                     |
| در دنیای واقعی                   | کمتر استفاده می‌شود                            | بیشتر استفاده می‌شود               |
| دستور در FTP                     | `PORT`                                         | `PASV` یا `EPSV`                   |

| وضعیت شبکه / مشکل                             | چه مود استفاده کنی    | دلیل                                                                       |
| --------------------------------------------- | --------------------- | -------------------------------------------------------------------------- |
| پشت **NAT** هستی (مثل اکثر VMها)              | **Passive**           | چون کلاینت اتصال دیتا را شروع می‌کند و NAT راحت‌تر اجازه می‌دهد            |
| پشت **Firewall سخت‌گیرانه** هستی              | **Passive**           | firewall معمولاً اتصال خروجی را اجازه می‌دهد ولی اتصال ورودی از سرور را نه |
| FTP server اجازه passive نمی‌دهد              | **Active**            | بعضی سرورها passive را غیرفعال می‌کنند                                     |
| انتقال فایل در Passive خطای `425` می‌دهد      | **Active** را تست کن  | ممکن است پورت‌های passive بسته باشند                                       |
| انتقال فایل در Active خطای `425` می‌دهد       | **Passive** را تست کن | معمولاً firewall اتصال ورودی را بسته                                       |
| داخل **LAN ساده بدون firewall** هستی          | هر دو کار می‌کند      | شبکه مستقیم است                                                            |
| در **Pentest از Kali داخل VM** استفاده می‌کنی | معمولاً **Passive**   | چون VM و NAT بین تو و سرور هستند                                           |
| وقتی directory listing کار نمی‌کند            | Passive را تست کن     | گاهی فقط passive درست کار می‌کند                                           |
| وقتی upload/download گیر می‌کند               | مود را عوض کن         | بعضی سرورها فقط یکی از مودها را درست پشتیبانی می‌کنند                      |