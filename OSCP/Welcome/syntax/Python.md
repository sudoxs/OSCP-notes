
###  venv

```python
python -m venv myenv
```

---

### فعالش کن

```pyhton
source myenv/bin/activate
```

باید ببینی:

`(myenv) kali@kali:~$`

---

### pip رو درست و تمیز کن

```pyhton
python -m ensurepip --upgrade
pip install --upgrade pip setuptools wheel
```


```python
sudo apt install python3-wsgidav
```


---

# Server

```pyhton
python3 -m http.server 8000
```

# pipx
```bash
sudo apt install pipx -y
pipx ensurepath
```
install:
```bash
pipx install xxxx
```